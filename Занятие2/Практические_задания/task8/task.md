Оператор * может использоваться не только для запаковки аргументов функции,  
но и их распаковки.
Например,
```python
tuple_ = (1, 2, 3)

print(tuple_)  # передаем кортеж как единое целое
print(*tuple_)  # передаем значения кортежа, оператор * распаковывает последовательность
```
